<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/playerRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:clickable="true"
    android:focusable="true">

    <!-- Top Section: Curved Mars Dome with Album Art -->
    <com.rafa.play.views.MarsCurvedHeaderLayout
        android:id="@+id/marsCurvedHeader"
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:layout_alignParentTop="true">

        <!-- Full Album Art Image filling the dome -->
        <ImageView
            android:id="@+id/ivPlayerArt"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:background="@color/surface_card"
            android:src="@drawable/ic_music_minimal" />

        <!-- Gradient overlay -->
        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/shadow_gradient_overlay" />

        <!-- Top Action Bar -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="32dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <ImageButton
                android:id="@+id/btnClosePlayer"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:layout_alignParentStart="true"
                android:layout_centerVertical="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_down_minimal"
                app:tint="@color/text_primary" />

            <TextView
                android:id="@+id/tvPlayerTopTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:ellipsize="end"
                android:maxWidth="200dp"
                android:maxLines="1"
                android:text="Rafa Play"
                android:textColor="#CCFFFFFF"
                android:textSize="12sp" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:orientation="horizontal">

                <!-- Lyrics toggle button -->
                <ImageButton
                    android:id="@+id/btnToggleLyrics"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_lyrics_minimal"
                    app:tint="#CCFFFFFF" />

                <!-- Orbit queue toggle button -->
                <ImageButton
                    android:id="@+id/btnToggleOrbit"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:layout_marginStart="4dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_orbit_minimal"
                    app:tint="#CCFFFFFF" />
            </LinearLayout>
        </RelativeLayout>

        <!-- Full Synchronized Lyrics Overlay (Toggled) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvLyrics"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="88dp"
            android:layout_marginBottom="44dp"
            android:background="#E6000000"
            android:visibility="gone" />

        <!-- Orbital Discovery Queue View Overlay (Toggled) -->
        <com.rafa.play.views.PlanetQueueView
            android:id="@+id/planetQueueView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="80dp"
            android:background="#E6000000"
            android:visibility="gone" />

    </com.rafa.play.views.MarsCurvedHeaderLayout>

    <!-- Edge-to-Edge Curved Seekbar running directly on the curved separation -->
    <com.rafa.play.views.MarsCurvedEdgeSeekBar
        android:id="@+id/marsCurvedEdgeSeekBar"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_alignBottom="@id/marsCurvedHeader"
        android:layout_marginBottom="-28dp" />

    <!-- Bottom Half: Metadata, Live Lyric Preview, and Playback Controls -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/marsCurvedEdgeSeekBar"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:paddingStart="24dp"
        android:paddingTop="12dp"
        android:paddingEnd="24dp"
        android:paddingBottom="24dp">

        <!-- Track Title and Artist -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvPlayerTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text_primary"
                android:textSize="21sp"
                android:textStyle="bold"
                tools:text="Song Title" />

            <TextView
                android:id="@+id/tvPlayerArtist"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/text_secondary"
                android:textSize="14sp"
                tools:text="Artist Name" />

            <!-- Real-time current synchronized lyric line preview -->
            <TextView
                android:id="@+id/tvCurrentLyricLine"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:ellipsize="end"
                android:gravity="center"
                android:maxLines="1"
                android:textColor="@color/accent_mars"
                android:textSize="14sp"
                android:visibility="gone"
                tools:text="♪ Current lyrics singing here ♪" />
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />

        <!-- Bottom Controls Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="18dp"
            android:gravity="center"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/btnShuffle"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_shuffle_minimal"
                app:tint="@color/text_muted" />

            <ImageButton
                android:id="@+id/btnPrev"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="16dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_skip_prev_minimal"
                app:tint="@color/text_primary" />

            <!-- Glowing Play / Pause button -->
            <FrameLayout
                android:id="@+id/btnPlayPauseWrapper"
                android:layout_width="64dp"
                android:layout_height="64dp"
                android:layout_marginStart="20dp"
                android:layout_marginEnd="20dp"
                android:background="@drawable/shape_glow_ring">

                <ImageButton
                    android:id="@+id/btnPlayerPlayPause"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_play_minimal"
                    app:tint="@color/text_primary" />
            </FrameLayout>

            <ImageButton
                android:id="@+id/btnNext"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginEnd="16dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_skip_next_minimal"
                app:tint="@color/text_primary" />

            <ImageButton
                android:id="@+id/btnRepeat"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_repeat_minimal"
                app:tint="@color/text_muted" />

        </LinearLayout>

    </LinearLayout>

</RelativeLayout>
